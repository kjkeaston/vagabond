<div class="container">
  <h2> <%= @user.first_name %>, add a new post!</h2>
  <hr>
  <br>
  <div class="container">
    <%= form_for @post do |f| %>
      <div class="form-group">
        <%= f.label :title, "Title" %>
        <%= f.text_field :title, placeholder: "Title", class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :content, "Content" %>
        <%= f.text_area :content, placeholder: "Content", class: "form-control" %>
      </div>
      <%= f.submit "Add Post", class: "btn btn-outline-primary" %>
      <%= link_to "Cancel", cities_path, class: "btn btn-outline-warning"%>
    <% end %>
  </div>
</div>
<br>

<!-- new_user_post_path(@user) -->